import CloseIcon from "@mui/icons-material/Close";
const CloseButton = () => {
  return (
    <CloseIcon
      sx={{ color: "var(--primary)", width: "50px", height: "50px" }}
    />
  );
};

export default CloseButton;
